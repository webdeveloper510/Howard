import {
  dom_default
<<<<<<< HEAD
} from "./chunk-MOVV2AV2.js";
=======
} from "./chunk-ASGLTZR4.js";
>>>>>>> 48adf44670fa9e24cf9b4618ce076bd54f56618d
import "./chunk-S5KM4IGW.js";

// node_modules/@left4code/tw-starter/dist/js/accordion.js
(function(dom) {
  "use strict";
  dom("body").on("click", "[data-tw-toggle='collapse']", function() {
    toggle(this);
  });
  function toggle(el, event = "toggle") {
    dom(el).closest(".accordion").find("[data-tw-toggle='collapse']").each(function() {
      if (!dom(this).hasClass("collapsed") && this !== el && (event == "toggle" || event == "hide")) {
        const event2 = new Event("hide.tw.accordion");
        dom(this).closest(".accordion-header")[0].dispatchEvent(event2);
      }
      if (dom(this).hasClass("collapsed") && this === el && (event == "toggle" || event == "show")) {
        const event2 = new Event("show.tw.accordion");
        dom(this).closest(".accordion-header")[0].dispatchEvent(event2);
      }
    });
    const collapsed = dom(el).hasClass("collapsed");
    dom(el).closest(".accordion").find(".accordion-collapse").slideUp(300, (el2) => {
      dom(el2).removeClass("show");
      dom(el2).closest(".accordion-item").find("[data-tw-toggle='collapse']").addClass("collapsed").attr("aria-expanded", false);
    });
    setTimeout(() => {
      const accordionCollapse = dom(el).closest(".accordion-item").find(".accordion-collapse");
      const hide = () => {
        dom(accordionCollapse).removeClass("show");
        dom(el).addClass("collapsed").attr("aria-expanded", false);
        dom(el).closest(".accordion-item").find(".accordion-collapse").slideUp();
      };
      const show = () => {
        dom(accordionCollapse).addClass("show");
        dom(el).removeClass("collapsed").attr("aria-expanded", true);
        dom(el).closest(".accordion-item").find(".accordion-collapse").slideDown();
      };
      if (event === "toggle") {
        !collapsed ? hide() : show();
      } else if (event === "show") {
        show();
      } else {
        hide();
      }
    }, 300);
  }
  function createInstance(el) {
    return {
      show() {
        toggle(el, "show");
      },
      hide() {
        toggle(el, "hide");
      },
      toggle() {
        toggle(el);
      }
    };
  }
  (function init() {
    dom("[data-tw-toggle='collapse']").each(function() {
      dom(this).closest(".accordion-header")[0]["__accordion"] = createInstance(this);
    });
    if (window.tailwind === void 0)
      window.tailwind = {};
    window.tailwind.Accordion = {
      getInstance(el) {
        return el.__accordion;
      },
      getOrCreateInstance(el) {
        return el.__accordion === void 0 ? createInstance(el) : el.__accordion;
      }
    };
  })();
})(dom_default);
//# sourceMappingURL=@left4code_tw-starter_dist_js_accordion.js.map
