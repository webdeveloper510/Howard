import {
  dom_default
<<<<<<< HEAD
} from "./chunk-MOVV2AV2.js";
=======
} from "./chunk-ASGLTZR4.js";
>>>>>>> 48adf44670fa9e24cf9b4618ce076bd54f56618d
import "./chunk-S5KM4IGW.js";

// node_modules/@left4code/tw-starter/dist/js/tab.js
(function(dom) {
  "use strict";
  dom("body").on("click", "[role='tab']", function() {
    show(this);
  });
  function show(el) {
    dom(el).closest("[role='tablist']").find("[role='tab']").each(function() {
      if (dom(this).hasClass("active") && this !== el) {
        const event = new Event("hide.tw.tab");
        dom(this)[0].dispatchEvent(event);
      }
      if (!dom(this).hasClass("active") && this === el) {
        const event = new Event("show.tw.tab");
        dom(this)[0].dispatchEvent(event);
      }
    });
    dom(el).closest("[role='tablist']").find("[role='tab']").removeClass("active").attr("aria-selected", false);
    dom(el).addClass("active").attr("aria-selected", true);
    let elementId = dom(el).attr("data-tw-target");
    let tabContentWidth = dom(elementId).closest(".tab-content").width();
    dom(elementId).closest(".tab-content").children(".tab-pane").removeAttr("style").removeClass("active");
    dom(elementId).css("width", tabContentWidth + "px").addClass("active");
  }
  (function onResized(el) {
    addEventListener("resize", (event) => {
      dom("[role='tabpanel']").each(function() {
        if (dom(this).hasClass("active")) {
          let tabContentWidth = dom(this).closest(".tab-content").width();
          dom(this).css("width", tabContentWidth + "px");
        }
      });
    });
  })();
  function createInstance(el) {
    return {
      show() {
        show(el);
      }
    };
  }
  (function init() {
    dom("[role='tab']").each(function() {
      this["__tab"] = createInstance(this);
    });
    if (window.tailwind === void 0)
      window.tailwind = {};
    window.tailwind.Tab = {
      getInstance(el) {
        return el.__tab;
      },
      getOrCreateInstance(el) {
        return el.__tab === void 0 ? createInstance(el) : el.__tab;
      }
    };
  })();
})(dom_default);
//# sourceMappingURL=@left4code_tw-starter_dist_js_tab.js.map
